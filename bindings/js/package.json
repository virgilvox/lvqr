{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @lvqr/core && npm run build -w @lvqr/player && npm run build -w @lvqr/dvr-player && npm run build -w @lvqr/admin-ui",
    "test:e2e": "playwright test",
    "test:sdk": "vitest run",
    "test:admin-ui": "npm --workspace @lvqr/admin-ui run test:unit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}

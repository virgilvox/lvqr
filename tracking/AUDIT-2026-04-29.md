# LVQR end-to-end audit (2026-04-29)

Author: Moheeb Zara <hackbuildvideo@gmail.com>

## Status update -- 2026-04-30 session 166 close

Three more findings landed in the follow-up session, one defer, and
the door closed on the second-priority chunk of remaining work.
Commits in landing order:

- **C-3 DASH availabilityStartTime + UTCTiming + publishTime** --
  `05a2268` `fix(dash)`. `Mpd` gains four optional timing fields;
  the renderer emits `availabilityStartTime`, `publishTime`, and
  `timeShiftBufferDepth` on the MPD root and a
  `<UTCTiming schemeIdUri="urn:mpeg:dash:utc:direct:2014"/>`
  descriptor after the Period. `DashServer` captures the wall-clock
  anchor lazily on first producer push via `ensure_started` (atomic
  compare-exchange so only the first caller wins, keeping the
  anchor constant across MPD re-renders inside a session). 5 new
  tests.
- **I-4 RTSP 415 on unsupported broadcaster codec** -- `e9ecdbe`
  `fix(rtsp)`. `handle_play` now computes both `is_h264` and
  `is_hevc` (and both `is_aac` / `is_opus`) and returns RFC 2326
  §11.3.16 `415 Unsupported Media Type` when the client requested
  a track via SETUP and the broadcaster's init bytes are not in
  the supported codec set. Adds `Response::unsupported_media_type`
  constructor.
- **I-5 RTMP unsupported codec_id warn + counter** --
  `bfee438` `fix(rtmp)`. `onMetaData` carries Adobe FLV `codec_id`
  values (`Option<u32>`); video != 7 (AVC) or audio != 10 (AAC)
  now warn at startup and increment
  `lvqr_rtmp_unsupported_codec_total{kind, codec_id}`. Hard reject
  via `session.publish_rejected` + `onStatus(error)` documented as
  still-open (the right long-term shape but needs more `rml_rtmp`
  surgery).
- **B-5 CMAF styp box** -- DEFERRED. Four byte-equality assertions
  (`bytes[4..8] == "moof"`) in `rtmp_archive_e2e`,
  `playback_signed_url_e2e`, `rtmp_bridge_integration` would need
  updating, and the archive recorder writes coalescer output as a
  single MP4 file -- prepending styp to every fragment changes the
  on-disk layout in a way that may confuse simple demuxers. The
  spec-correct fix is to add styp at the HLS / DASH partial-serving
  layer (where each chunk IS a standalone deliverable), not at
  `build_moof_mdat`. That refactor is its own session.

Remaining open after session 166 (priority order):

- **C-2 WHEP PLI / FIR** -- large cross-crate; needs an upstream
  keyframe-request signal path through
  `FragmentBroadcasterRegistry`.
- **C-6 WHIP / WHEP 415 on incompatible-codec offer** -- medium;
  needs an `accept_offer` codec validator that intersects the
  publisher's media against the bridge's accepted set BEFORE the
  201 Created lands.
- **C-9 WHEP AAC -> Opus drop on non-transcode builds** -- medium;
  detect publisher-side AAC at session-start and return HTTP 422
  on the WHEP POST when the build does not include the
  AAC -> Opus transcoder.
- **I-1 WHIP trickle ICE PATCH** -- medium; thread the parsed
  candidate fragment into `str0m::Rtc::add_remote_candidate`.
- **I-2 WHEP `extract_whip` rename** -- small; cosmetic.
- **I-5b RTMP onStatus(error) hard reject** -- the deeper companion
  to I-5; needs `rml_rtmp` surgery.
- **I-6 WHEP rtcp-fb consumer** -- tied to C-2.
- **I-8 Federation imported-broadcasts visibility** -- small.
- **B-5 CMAF styp at chunk-serving layer** -- small but cross-crate;
  see the defer note above.
- **B-6 / B-7 server-info `auth_mode` classifier + `config_path`**
  -- small.

## Status update -- 2026-04-29 session 165 close

Six findings from this audit landed in the same session. Two were
small enough to close directly; three were medium but contained;
one (I-3 + I-7) bundled both observability gaps. Commits, in
landing order:

- **C-1 CORS expose Location** -- `5e99a4b` `fix(cors)`. Replaces
  `CorsLayer::permissive()` on every browser-facing router with an
  explicit layer that exposes `Location`, `Content-Type`, `ETag`.
  Unblocks browser-side WHIP/WHEP PATCH + DELETE.
- **C-5 HEVC compatibility-flags bit reversal** -- `a3d3203`
  `fix(codec)`. `HevcSps::codec_string` now emits the bit-reversed
  `general_profile_compatibility_flags` per ISO/IEC 14496-15 annex
  E. Conformance fixtures + 3 in-tree test assertions updated
  alongside; new `codec_string_reverses_compat_flag_bit_order` test
  locks the reversal in explicitly.
- **C-4 HLS TARGETDURATION ceil** -- `2a7affe` `fix(hls)`. Renderer
  now treats configured `target_duration_secs` as a floor and
  emits `max(configured, ceil(observed))`. New test
  `render_target_duration_ceils_observed_segment_when_over_configured`.
- **C-7 HLS EXT-X-DISCONTINUITY on init swap** -- `9e2f21b`
  `fix(hls)`. `PlaylistBuilder::mark_discontinuity_pending` latch
  + new `Segment.discontinuity` field + render emission.
  `HlsServer::push_init` flips the latch on every replacement init
  (publisher reconnect on the same broadcast slot). Two new tests
  cover the latch semantics and the end-to-end wiring.
- **I-3 SRT unknown-stream-type warn + counter** + **I-7 WHEP
  codec-mismatch counter** -- `ca58e47` `fix(observability)`. Two
  observability gaps closed together: SRT unknown stream_type now
  one-shot warns + always increments
  `lvqr_srt_unknown_stream_type_drops_total`; WHEP codec
  mismatches always increment
  `lvqr_whep_codec_mismatch_drops_total{broadcast, codec, reason}`.
- **B-1 + B-2 WHEP default port doc sweep** -- `30699e4`
  `docs(ports)`. Admin UI placeholder + advanced-hint, every
  example in `docs/quickstart.md`, `docs/deployment.md`, and
  `README.md` flipped from `8443` to `8444` for the WHEP-side
  examples. Adds a one-line note explaining why WHIP and WHEP need
  distinct ports.

Two earlier commits in the same session did the foundational
work the audit was scoped on top of:

- `feat(admin)` `0a7cb67` -- `GET /api/v1/server-info` route wired
  end-to-end (the WIP from session 164 the briefing flagged as
  needing follow-through).
- `fix(admin-ui)` `59b1291` -- DVR view's HLS URL composition (the
  WIP that surfaced the URL-composition bug class in the first
  place).

Still open from this audit (priority order, suitable for a future
session):

- **C-2 WHEP PLI / FIR feedback wiring**. Cross-crate; needs an
  upstream keyframe-request signal path through
  `FragmentBroadcasterRegistry` so the publisher can rotate.
- **C-3 DASH availabilityStartTime + UTCTiming + publishTime**.
  Medium; needs a wall-clock anchor stashed on the per-broadcast
  DASH state at first-segment time and threaded through MPD
  emission.
- **C-6 WHIP / WHEP 415 on incompatible-codec offer**. Medium;
  needs an `accept_offer` codec validator that intersects the
  publisher's media against the bridge's accepted set.
- **C-9 WHEP AAC -> Opus drop on non-transcode builds**. Medium;
  detect publisher-side AAC at session-start and return HTTP 422
  on the WHEP POST when the build does not include the AAC->Opus
  transcoder.
- **I-1 WHIP trickle ICE PATCH**. Medium; thread the parsed
  candidate fragment into `str0m::Rtc::add_remote_candidate`
  rather than logging + discarding.
- **I-2 WHEP `extract_whip` rename**. Small; cosmetic but worth
  renaming so future divergence does not silently regress one
  side.
- **I-4 RTSP unknown-codec 415**. Small; return RTSP 415 in
  DESCRIBE when the broadcaster's init segment is not AVC or
  HEVC, instead of silently spawning the H.264 drain.
- **I-5 RTMP video_codec_id validation**. Small; reject
  `onMetaData` with non-7 video_codec_id at handshake time.
- **I-6 WHEP rtcp-fb consumer**. Medium; tied to C-2.
- **I-8 Federation imported-broadcasts visibility**. Small;
  attach an "imported broadcasts" tally to the existing
  federation status surface.
- **B-5 CMAF styp marker on each chunk**. Small; emit the
  optional `styp` box at the start of every coalesced moof+mdat.
- **B-6 + B-7 server-info auth_mode classifier + config_path**.
  Small; needs ServeConfig to thread the auth-boot classifier +
  the parsed config-file path.

Three classes of fix were declared "medium-large" in the original
audit and remain there; the audit document below captures
file:line evidence + a sized fix plan for each.



Premise: workspace tests are green and v1.0.0 shipped to crates.io / npm /
PyPI / GitHub Releases / ghcr.io, but real operators cannot use the product
end-to-end. Concrete failures the user has hit (DVR scrubber `manifestLoadError`
from hls.js because of an admin-port composition bug; WHIP/WHEP silent VP8
drop because the str0m bridge only handles H.264 + HEVC; `/api/v1/streams`
querying the RTMP bridge instead of `FragmentBroadcasterRegistry`; default
port collision between WHIP and WHEP at 8443) all point at the same shape:
correct in isolation, wrong when wired together.

This document is the audit deliverable from session 165 (2026-04-29). It
combines code reading across every protocol crate, spec citations against the
authoritative IETF / W3C / ISO documents, and competitor cross-references.
It does **not** include a live reproduction matrix run against real iOS
Safari + OBS + ffmpeg + tcpdump; the matrix template is filled in below
based on the code-reading evidence with `code-evidence` cells, and an
operator follow-up session should re-run it on real wire (see
`Verification matrix` section for the recipe).

## How to read this document

- **Critical**: broken in practice, blocks operator use today.
- **Important**: correct in isolation, fragile under load or reconnection.
- **Backlog**: improvements that increase player compatibility or operator UX.
- **Verification matrix**: ingest x subscribe x player. Cells filled from
  code-reading; flag the genuinely tested ones with `wire-tested` once a
  real reproduction session is run.
- **Spec scorecard**: one row per protocol MUST/SHOULD; compliant / partial /
  non-compliant / not-applicable with citations.
- **Competitor gap analysis**: top three competitor features per protocol
  we lack.

Every finding cites file:line. Every fix is sized as either
`small (commit directly)` or `large (document, hand off)`.

## Critical

### C-1. WHIP/WHEP `Location` header unreadable from browser JS (CORS)

**Spec violation**: WHIP draft-ietf-wish-whip-15 §4.1 (and the same in
WHEP). The 201 Created response carries `Location:` pointing at the
session resource URL; the client uses that URL for PATCH (trickle ICE)
and DELETE (terminate). The browser Fetch / XHR layers refuse to expose
non-CORS-safelist response headers to JS unless the server lists them in
`Access-Control-Expose-Headers`. `Location` is **not** on the safelist.

**Evidence**: `crates/lvqr-cli/src/lib.rs` lines 1457 + 1477 mount
`CorsLayer::permissive()` on the WHIP and WHEP routers.
`tower_http::cors::CorsLayer::permissive()` permits the request side
(`allow_origin: any`, `allow_headers: any`, `allow_methods: any`) but
sets the response side `expose_headers` to **empty**. A browser-side
WHIP client can POST the offer and receive 201 + Location, but its JS
cannot read the header value, so PATCH and DELETE land on the wrong
URL or never fire.

**Why workspace tests do not catch it**: every WHIP / WHEP integration
test in the tree drives the protocol from a Rust client (`crates/lvqr-cli/tests/whip_hls_e2e.rs`,
`rtmp_whep_audio_e2e.rs`) where CORS does not gate header visibility.
The browser admin-ui's `useWhipPublisher.ts` happens to work today
because Chrome's WHIP draft implementation is permissive, but this is
incidental.

**Fix**: small (under 30 lines). Replace `CorsLayer::permissive()` on
the WHIP / WHEP / signal / HLS / DASH routers with an explicit layer:

```rust
use tower_http::cors::{Any, CorsLayer};
use http::header;
let cors = CorsLayer::new()
    .allow_origin(Any)
    .allow_methods(Any)
    .allow_headers(Any)
    .expose_headers([header::LOCATION, header::CONTENT_TYPE, header::ETAG]);
```

`ETAG` is added because the WHIP draft suggests `If-Match` for SDP
renegotiation, which requires a readable `ETag` on the 201 response.

### C-2. WHEP egress has no PLI / FIR keyframe-request feedback

**Spec violation**: WHEP draft-ietf-wish-whep-13 §4.2 mandates
RTCP feedback for keyframe requests. Without PLI / FIR, a viewer that
joins mid-stream waits for the next IDR (potentially up to GOP length)
before seeing any video, and bandwidth-drop recovery (decoder asks for
a refresh keyframe) never reaches the publisher.

**Evidence**: `crates/lvqr-whep/src/str0m_backend.rs` event loop (around
lines 689-706) handles `IceConnectionStateChange`, `Connected`,
`MediaAdded`, but has **no handler for `Event::KeyframeRequestRecv`**
or anything that proxies a PLI back through the
`FragmentBroadcasterRegistry` to the publisher. The SDP answer
advertises `a=rtcp-fb:* nack pli` but the inbound feedback events are
discarded.

**Fix**: large (cross-crate). Need a path from `str0m::Event::KeyframeRequest`
on the WHEP session to an upstream `KeyframeRequest` signal on the
publisher's ingest path. RTMP: there is no native PLI in RTMP, but we
can rotate the encoder via the FLV `onFI` mechanism; in practice
`FragmentBroadcaster` owns the encoder and can emit a "next sample
must be IDR" hint that the WHIP / RTMP / SRT ingest paths honor.

### C-3. DASH MPD missing `availabilityStartTime`, no `UTCTiming` element

**Spec violation**: ISO/IEC 23009-1:2022 §5.3.1.2: for live
(`type="dynamic"`) MPDs, `availabilityStartTime` is required, and a
`UTCTiming` descriptor (or the `availabilityStartTime` plus a
synchronized clock) is required for clock alignment.

**Evidence**: `crates/lvqr-dash/src/mpd.rs:417-468` emits the `<MPD>`
element with `type` and `mediaPresentationDuration` but no
`availabilityStartTime` attribute and no `<UTCTiming>` child.

**Player impact**: dash.js, Shaka, ExoPlayer all refuse to attach a
live presentation timeline to a dynamic MPD without this. The MPD
either parses as VOD-styled (no live edge) or the player drops out
with a "missing AST" error.

**Fix**: large. Need to thread the wall-clock anchor (the moment the
encoder began producing the first sample of the presentation) through
the bridge and emit `availabilityStartTime` as
`format!("{}", chrono::Utc::now().to_rfc3339_opts(SecondsFormat::Millis, true))`.
For low-latency DASH, also emit `availabilityTimeOffset` per
representation.

### C-4. HLS `EXT-X-TARGETDURATION` not derived from real segment durations

**Spec violation**: RFC 8216bis §4.4.3.1: TARGETDURATION must be the
**ceiling** of the longest segment in the playlist, in integer seconds.

**Evidence**: `crates/lvqr-hls/src/manifest.rs:258` writes
`#EXT-X-TARGETDURATION:{}` from `self.target_duration_secs` (a `u32`
configured at builder construction). The builder never inspects the
durations of pushed segments to recompute. If a segment is 2.001 s but
config says 2 s, strict players reject the manifest with
`MediaError: HLS_MANIFEST_PARSING_ERROR`.

**Player impact**: hls.js logs a fatal `MANIFEST_PARSING_ERROR` and
stops; iOS Safari (the strictest production HLS client) fails the
playback altogether.

**Fix**: medium (single file). Track the max-observed `partial_target *
parts_per_segment` rounded to integer seconds, ceil it, and emit that
instead of the configured value. Backwards-compatible: the config
value becomes a floor.

### C-5. HEVC codec string emits `general_profile_compatibility_flags` in the wrong bit order

**Spec violation**: RFC 7798 §3.2 (HEVC RTP payload format,
`HEVCDecoderConfigurationRecord` codec-string mapping) and ISO/IEC
14496-15 §8.3 specify the compat-flags field is serialised in
**reverse bit order** (LSB-first) into the codec string. `format!(":X",
...)` outputs the field MSB-first.

**Evidence**: `crates/lvqr-codec/src/hevc.rs:120-134`:

```rust
pub fn codec_string(&self) -> String {
    format!(
        "hev1.{}{}.{:X}.L{}.B0",
        match self.general_profile_space { ... },
        self.general_profile_idc,
        self.general_profile_compatibility_flags,
        self.general_level_idc,
    )
}
```

**Player impact**: HEVC publishers feeding Shaka / dash.js are
rejected with codec-not-supported. iOS Safari is more forgiving (uses
`hvc1` sample-entry only) so this can hide depending on which player
the operator tests against.

**Fix**: small (under 30 lines). Bit-reverse the 32-bit field before
the `{:X}` format. Add a unit test against a known SPS (e.g. the one
in `crates/lvqr-codec/tests/fixtures/`).

### C-6. WHIP / WHEP accept SDP offer with unsupported codec, then silently drop

**Spec violation / interop risk**: WHIP draft §3.1: when the server
cannot answer an offer compatible with its capabilities, it should
return 4xx (typically 415 Unsupported Media Type) or answer with
`a=inactive` on the offending m-line.

**Evidence**: `crates/lvqr-whip/src/str0m_backend.rs` accept_offer
(lines 81-146) builds an answer from str0m, which silently negotiates
out codecs the bridge does not handle. The session POST returns 201,
the publisher sees ICE connect, but no MediaData ever flows because
the bridge's `MediaCodec` match (line 352-359) drops anything other
than H.264 / HEVC. The recently-added browser-side `setCodecPreferences`
fix (commit d14b726) papered over this but does not address the
root cause.

`crates/lvqr-whep/src/str0m_backend.rs:798-818` has the same shape:
audio AAC -> Opus-only subscriber drops, video H.265 -> H.264-only
subscriber drops, with a `tracing::warn!` that fires once per session
then silences.

**Fix**: medium. Add a pre-answer codec validator that intersects the
offer's codecs with the bridge's accepted set; if empty, return HTTP
415. Same on WHEP (the answerer should refuse if no compatible codec
is present in the publisher).

### C-7. HLS `EXT-X-DISCONTINUITY` not emitted when publisher reconnects with new init

**Spec violation**: RFC 8216bis §4.4.4.4: a discontinuity tag MUST
appear when the codec or encoding parameters of consecutive segments
differ.

**Evidence**: `crates/lvqr-cli/src/hls.rs:146-167` detects an init
segment change and calls `server.push_init(...)` + resets
`CmafPolicyState`; **but** `crates/lvqr-hls/src/manifest.rs:611-632`
keeps incrementing `next_sequence` monotonically and never emits
`#EXT-X-DISCONTINUITY`. Strict clients (hls.js, Shaka) glitch or
fail playback at the boundary.

**Fix**: small (under 30 lines). Add a `discontinuity: bool` field on
the next-segment-to-emit row inside `PlaylistBuilder` and have the
HLS bridge set it when `current_init != last_init`. Render path: emit
`#EXT-X-DISCONTINUITY` immediately before the segment URI line.

### C-8. Live HLS / DASH `CorsLayer` posture inconsistent

**Spec context**: Apple HLS authoring spec + DASH-IF guideline both
require `Access-Control-Allow-Origin: *` (or equivalent) on every
manifest + segment URL when the player is browser-side.

**Evidence**: `crates/lvqr-hls/src/server.rs:7-9` documents:

```
//! no CORS (the deployment adds that at the reverse
//! proxy), no auth (auth lives in `lvqr-auth` and slots in via the
//! `lvqr-cli` serve path when HLS joins the composition root).
```

The CLI composition root mounts `CorsLayer::permissive()` on the
combined HLS + DASH router (line 1457). Permissive is fine for the
mainline browser-fetch case but **does not expose `Access-Control-Allow-Headers`
on the actual HLS / DASH 200 responses for non-OPTIONS verbs**, which
in practice is fine because GET / HEAD never need custom headers.

**Conclusion**: works for browser playback today. The risk is that the
`CorsLayer::permissive()` choice silently drops a future custom
header (e.g. `X-Lvqr-Latency-Hint`) needed by client SDKs. Annotate
the routers to use the explicit form from C-1 above.

### C-9. WHEP AAC -> Opus mismatch silently drops audio when `transcode` feature is off

**Evidence**: `crates/lvqr-whep/src/str0m_backend.rs:798-804` warns
once per session then drops every subsequent AAC sample. RTMP-publisher
to WHEP-subscriber on a build without `--features transcode` produces
a video-only stream; the operator sees `ON AIR` but no audio.

**Fix**: medium. The `--features transcode` builds already ship the
session 113 AAC->Opus path. For non-transcode builds: emit an explicit
HTTP 422 on the WHEP POST when the publisher is AAC-only and the
session would have to drop audio.

## Important

### I-1. WHIP trickle ICE PATCH is logged but not applied

**Evidence**: `crates/lvqr-whip/src/str0m_backend.rs:465-470` --
PATCH body is parsed, candidate fragments are extracted, but the
candidate is **logged at warn level and discarded**. The session
proceeds with whatever ICE candidates the original offer carried.

**Impact**: a publisher behind a NAT that needs to send mid-session
candidates (network change, Wi-Fi to LTE) cannot do so. WHIP draft
§4.2 says trickle SHOULD be supported.

**Fix**: medium. str0m does support post-init `add_remote_candidate`;
wire the PATCH path to it.

### I-2. WHEP router calls `extract_whip()` for auth

**Evidence**: `crates/lvqr-whep/src/router.rs:99` calls the function
named `extract_whip`. Functional today (both protocols extract the
Bearer header the same way) but a future change to WHIP-side auth
that does not apply to WHEP would silently regress WHEP.

**Fix**: small. Rename `extract_whip` to `extract_session_token` (or
similar protocol-agnostic name) and re-export under both legacy
names for one release cycle.

### I-3. SRT unknown TS stream type silently dropped at debug level

**Evidence**: `crates/lvqr-srt/src/ingest.rs:248-250` logs
`debug!("SRT unknown stream type; dropping PES")`. Operators running
without `RUST_LOG=debug` see no warning when SRT carriage of HEVC
(stream_type 0x24) or AV1 (0x42) is dropped.

**Fix**: small. Elevate to `warn!` and rate-limit (one per stream_type
per minute per broadcast).

### I-4. RTSP unknown rtpmap codec silently falls back to H.264 drain

**Evidence**: `crates/lvqr-rtsp/src/server.rs:954-990` dispatches
between `play_drain_h264` and `play_drain_hevc` based on init segment
shape; for non-H.264 / non-HEVC, **falls back to H.264 unconditionally**.
Result: the depacketizer fails silently; PLAY appears to start but
emits nothing.

**Fix**: small. Return RTSP 415 in DESCRIBE when the broadcast init
is neither AVC nor HEVC.

### I-5. RTMP video_codec_id not validated against payload format

**Evidence**: `crates/lvqr-ingest/src/rtmp.rs:389` logs the metadata
but never rejects non-7 (H.264) values. Enhanced RTMP HEVC
(`fourCC: hvc1`) and AV1 (`fourCC: av01`) work today only because
the RTMP bridge's bytestream parsing is shape-agnostic; if a
publisher claims H.264 in metadata but pushes VP6, the bridge
forwards garbage to the fragment registry.

**Fix**: small. Validate codec_id against the payload's first byte
and reject mismatches via `onStatus(error)` RTMP message.

### I-6. WHEP / WHIP rtcp-fb advertised but unused

**Evidence**: WHEP SDP answer carries `a=rtcp-fb:* transport-cc nack pli`
but the str0m event loop ignores RTCP feedback events.
Bandwidth-aware ABR cannot land without consuming these.

### I-7. WHEP non-H.264/HEVC video silently logged-once at warn

**Evidence**: `crates/lvqr-whep/src/str0m_backend.rs:814-818`. After
the one-shot warn, every subsequent sample is discarded silently.
This is the same shape as the original VP8 silent-drop bug; the warn
is a soft compensation but the operator metric story is missing.

**Fix**: small. Increment a `lvqr_whep_codec_mismatch_drops_total`
counter on every drop, labeled by `(broadcast, codec)`. Operator
sees the drop rate on Prometheus.

### I-8. Federation runner observes only remote MoQ announcements

**Evidence**: `crates/lvqr-cluster/src/federation.rs:573-594` drains
the remote cluster's MoQ announcement stream. A federation-only
inbound broadcast does not appear in the local
`/api/v1/streams` response unless re-published locally. By design,
but undocumented.

**Fix**: small. Add a per-link "imported broadcasts" tally to the
federation status surface.

## Backlog

### B-1. Admin-ui `ConnectionDrawer.vue` placeholder for WHEP says 8443

**Evidence**: `bindings/js/packages/admin-ui/src/components/widgets/ConnectionDrawer.vue:153`
shows `placeholder="8443"` for the WHEP override field; canonical
default flipped to 8444 in commit 1e36d1a. Same hint text on line
145 says "RTMP 1935, WHIP/WHEP 8443" -- conflicts with the 8444
deconfliction.

**Fix**: small. Three string changes.

### B-2. README + `docs/quickstart.md` + `docs/deployment.md` carry old WHEP default port

**Evidence**: `README.md:406`, `docs/quickstart.md:46,100`,
`docs/deployment.md:33` use `--whep-port 8443` and
`https://localhost:8443/whep/...`. Operators following these recipes
hit the WHIP listener and silently fail.

**Fix**: small. Sed across docs.

### B-3. HLS line endings + version pinning are correct

LF (not CRLF), VERSION 9 hardcoded for LL-HLS, INDEPENDENT-SEGMENTS
emitted. No fix needed; logged here so the next audit can skip.

### B-4. fMP4 box ordering + 64-bit `tfdt` are correct

`crates/lvqr-cmaf/src/init.rs:70-197` (AVC), 230-394 (HEVC), 417-536
(AAC); ftyp -> moov(...mvex). `coalescer.rs:214-276` builds
moof+mdat in spec order. `tfdt.base_media_decode_time` is 64-bit
(version 1) per the comment at coalescer.rs:48-49. No fix.

### B-5. CMAF segments do not emit `styp` box

`crates/lvqr-cmaf/src/coalescer.rs:214-276` emits moof+mdat without
a leading `styp`. ISO/IEC 23000-19 §7.4 says `styp` is mandatory for
CMAF chunks (low-latency partials), optional for whole segments.
Adding it would make our chunked LL-HLS / LL-DASH delivery more
universally accepted.

**Fix**: small.

### B-6. `auth_mode` classifier in `/api/v1/server-info` is coarse

The wire-up landed in this session classifies as `"noop" |
"configured"`. The admin UI's Auth view would benefit from
finer-grained labels. The proper fix is to either grow
`AuthProvider::kind() -> &'static str` or thread the boot
classifier through `ServeConfig` directly. Documented in the
inline comment.

### B-7. `config_path` not surfaced in `/api/v1/server-info`

`ServeConfig` does not carry the parsed config-file path. The
admin UI can read it from `/api/v1/config-reload` (which already
includes `last_applied_path`), but a single round-trip would be
cleaner.

**Fix**: small. Add `pub config_path: Option<PathBuf>` to
`ServeConfig`, populate from `--config` in main.rs, thread into
the server_info closure.

## Verification matrix

Cells are filled by code-evidence inference, **not** real wire
testing. A `wire-tested` annotation marks cells the next operator
session re-runs against a real player. Recipe in the briefing
section "How to set up real-world reproduction".

|                                  | HLS hls.js | HLS Shaka | HLS Safari iOS | DASH dash.js | DASH Shaka | WHEP Chrome | RTMP mpv | SRT mpv | RTSP VLC | MoQ moq-rs |
|----------------------------------|------------|-----------|----------------|--------------|------------|-------------|----------|---------|----------|------------|
| WHIP via Chrome admin-ui         | FAIL (C-4, C-7) | FAIL (C-5 if HEVC) | PASS (TARGETDURATION ceil OK on Safari) | FAIL (C-3) | FAIL (C-3) | PASS (with C-1 fix; FAIL on browser today) | N/A | N/A | N/A | code-evidence PASS |
| WHIP via OBS Studio              | same as above | same | same | same | same | same | N/A | N/A | N/A | same |
| RTMP via OBS Studio              | same | same | same | same | same | FAIL (C-9 audio drop) | code-evidence PASS | N/A | N/A | code-evidence PASS |
| RTMP via ffmpeg                  | same | same | same | same | same | FAIL (C-9 audio drop) | code-evidence PASS | N/A | N/A | code-evidence PASS |
| SRT via ffmpeg                   | code-evidence PASS | needs C-4 fix | code-evidence PASS | FAIL (C-3) | FAIL (C-3) | FAIL (C-9 audio drop) | code-evidence PASS | code-evidence PASS | N/A | code-evidence PASS |
| RTSP via ffmpeg                  | code-evidence PASS | needs C-4 fix | code-evidence PASS | FAIL (C-3) | FAIL (C-3) | FAIL (C-9 audio drop, I-4) | N/A | N/A | code-evidence PASS | code-evidence PASS |

**Reading the matrix**: every WHIP/RTMP/SRT/RTSP -> DASH cell fails
because of C-3 (no `availabilityStartTime`). Every cell against Shaka
-> HLS fails when the source is HEVC (C-5). hls.js is more forgiving
than Shaka on the TARGETDURATION boundary but still fails on
discontinuity (C-7). Every cell against WHEP via Chrome fails on the
browser side because of C-1 (Location-header CORS) until that lands.

## Spec-conformance scorecard

Cited as `MUST` / `SHOULD` per the spec section. Compliance:
`Y` compliant, `P` partial, `N` non-compliant, `-` not applicable.

### WHIP (draft-ietf-wish-whip-15)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 4.1 | MUST | POST returns 201 with Location header | Y | router.rs:130 |
| 4.1 | MUST | 201 carries application/sdp body | Y | router.rs:134 |
| 4.1 | MUST | Bearer auth honored | Y | router.rs:112 |
| 4.1 | MUST | Browser-readable Location | **N** | C-1 |
| 4.2 | SHOULD | Trickle ICE via PATCH | **P** | I-1 (logged, not applied) |
| 4.3 | MUST | DELETE terminates session | Y | router.rs:166 |
| 5 | MUST | Reject incompatible offer (415 or a=inactive) | **N** | C-6 |

### WHEP (draft-ietf-wish-whep-13)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 4.1 | MUST | POST returns 201 with Location | Y | whep router.rs |
| 4.2 | MUST | Keyframe request via RTCP PLI/FIR | **N** | C-2 |
| 4.2 | SHOULD | rtcp-fb wired (transport-cc, nack pli) | **N** | I-6 |
| 5 | MUST | Reject session if no compatible codec | **N** | C-6 |

### HLS (RFC 8216bis)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 4.4.3.1 | MUST | TARGETDURATION = ceil(max segment duration) | **N** | C-4 |
| 4.4.4.4 | MUST | DISCONTINUITY on codec param change | **N** | C-7 |
| 4.4.5.1 | SHOULD | PROGRAM-DATE-TIME on every segment when CAN-SKIP-UNTIL set | **P** | manifest.rs:305 (only when base set) |
| 4.4.4.6 | MUST | INDEPENDENT-SEGMENTS when every segment starts at IDR | Y | manifest.rs:256 |
| 6.2 | MUST | LF line endings | Y | writeln! uses \n |
| 6.3 | MUST | EXT-X-VERSION reflects features used | Y | hardcoded 9 |

### DASH (ISO/IEC 23009-1:2022)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 5.3.1.2 | MUST | availabilityStartTime on dynamic MPD | **N** | C-3 |
| 5.3.1.2 | SHOULD | UTCTiming descriptor | **N** | C-3 |
| 5.3.4 | MUST | Codec strings RFC 6381 compliant | **P** | C-5 (HEVC reverse-bit-order) |

### CMAF (ISO/IEC 23000-19)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 7.4 | MUST | styp box on each chunk | **N** | B-5 |
| 7.5 | MUST | tfdt 64-bit baseMediaDecodeTime | Y | coalescer.rs:48 |
| 7.5 | MUST | sample is_sync_sample on IDR | Y | coalescer.rs:357 |

### RTMP (Adobe RTMP 1.0)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 5.4 | SHOULD | Reject mismatched video_codec_id | **N** | I-5 |
| 7.2.2 | MUST | AAC sequence header before raw audio | Y | bridge enforces |

### RTSP (RFC 7826 / 2326)

| § | MUST/SHOULD | Item | Compliance | Note |
|---|---|---|---|---|
| 12.35 | SHOULD | 415 on unsupported codec | **N** | I-4 |
| 13.4 | MUST | RTP timestamp monotonicity | Y | depacketizer enforces |

## Competitor-feature gap analysis

Surveying OvenMediaEngine, Mediasoup, LiveKit, SRS for protocol-level
features we lack and that operators routinely ask for.

### WHIP/WHEP

1. **OvenMediaEngine** ships full simulcast pass-through on WHIP and
   layer selection on WHEP. We have none of it; every WHEP subscriber
   gets the single quality the publisher pushes.
2. **LiveKit** ships server-side bandwidth estimation on WHEP and
   adaptive simulcast layer pinning. We do not consume any RTCP
   feedback (I-6).
3. **Mediasoup** treats codec capability as a first-class
   `RtpCapabilities` object that the client negotiates against
   before SDP is built. Our bridge does the negotiation inside
   str0m, which is why codec mismatches fail silently (C-6).

### HLS

1. **OvenMediaEngine** + **Mux** both ship LL-HLS with sub-300ms
   glass-to-glass on real ingests. Our LL-HLS scaffolding is in
   place (PART, PRELOAD-HINT, RENDITION-REPORT, CAN-BLOCK-RELOAD)
   but C-4 + C-7 mean strict players reject our output before
   measuring latency.
2. **SRS** ships HLS with `EXT-X-DATERANGE` for ad markers (SCTE-35
   passthrough). Anti-scoped per ROADMAP but worth re-evaluating.
3. **Apple HTTP Live Streaming Tools** ship a `mediastreamvalidator`
   binary; running it against our output in CI would catch C-4 +
   C-7 + most B-5 class issues before merge.

### DASH

1. **OvenMediaEngine** ships full live LL-DASH with
   `availabilityTimeOffset` per Representation; we lack
   `availabilityStartTime` entirely (C-3).
2. **dash-IF dashif-conformance** Docker container is the
   authoritative MPD validator; PLAN_V1.1 rows 117/118 noted this
   as deferred. Time to wire it in.
3. **Shaka Streamer** ships chunked encoding with consistent
   `presentationTimeOffset` across rotations. Our DASH does not
   handle encoder rotation cleanly (no DISCONTINUITY equivalent).

## Definition of done -- where this audit lands

The audit is **partial**: it surfaces 9 critical items and 8 important
items via code reading + spec review. It does **not** include a
real-wire reproduction matrix; the cells above are filled from
code-reading evidence with explicit annotations.

What this session shipped (code-side):

- `GET /api/v1/server-info` admin route wired end-to-end
  (`crates/lvqr-admin/src/{lib,routes,server_info_routes}.rs` +
  `crates/lvqr-cli/src/lib.rs` composition root). Returns version +
  build features + bound listener addresses + runtime feature flags.
  3 unit tests pass. Workspace builds green; clippy clean on the
  changed crates.

What the next session should pick up (in priority order):

1. **C-1** (CORS expose Location). Smallest, blocks every
   browser-side WHIP/WHEP today.
2. **C-7** (HLS DISCONTINUITY on init swap). Small; smoothes the
   reconnect story.
3. **C-4** (HLS TARGETDURATION ceil). Small; unblocks Shaka.
4. **C-5** (HEVC codec-string bit reversal). Small; unblocks HEVC
   on Shaka + dash.js.
5. **C-3** (DASH availabilityStartTime + UTCTiming). Medium; unblocks
   every DASH cell in the matrix.
6. **C-2** (WHEP PLI/FIR). Large; cross-crate.
7. **C-6** (WHIP/WHEP 415 on codec mismatch). Medium; addresses the
   class of bug the user originally hit.

After every fix, re-run the matrix on real wire (Chrome admin-ui ->
HLS via hls.js, OBS -> RTMP -> WHEP via Chrome, ffmpeg -> SRT ->
DASH via dash.js). The matrix template above is the success bar.

# Work Notes - 2026-04-10

## Session 1: Project Bootstrap

### Completed
- Read and analyzed explainer.html (full LVQR architecture spec)
- Studied clasp repo structure as reference for monorepo patterns
- Verified all key dependencies exist on crates.io (moq-lite 0.15.9, quinn 0.11.9, etc.)
- Researched moq-lite API surface via docs.rs (Session, Origin, Broadcast, Track, Group, Frame hierarchy)
- Created comprehensive implementation plan (9 phases, 6 milestones)
- Built full project scaffolding (9 crates, workspace config, licenses, contributing guide)
- Implemented lvqr-core with 25 passing tests:
  - RingBuffer: circular buffer with Bytes ref-counting
  - GopCache: GOP retention for late-join with eviction
  - Registry: broadcast-based subscriber fanout
- Implemented lvqr-test-utils: port allocation, synthetic frames, test certs
- All workspace compiles, clippy clean (except expected stub warnings), fmt passing

### Key Learnings
- moq-lite uses Producer/Consumer pattern at each hierarchy level
- The relay inverts perspective: subscribes to what clients publish, publishes to subscribers
- rml_rtmp crate uses underscore (rml_rtmp), not hyphen
- moq-native requires Rust 1.85+ (edition 2024)
- DashMap v6 changed iter() return type, needs explicit type annotation

### Next Session
- Implement lvqr-relay with real MoQ session handling
- Wire up moq-native Server for WebTransport connections
- Write integration tests with actual QUIC connections
